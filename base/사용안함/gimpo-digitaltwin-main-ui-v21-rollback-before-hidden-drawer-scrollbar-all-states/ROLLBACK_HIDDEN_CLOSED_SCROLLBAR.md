# Rollback: hidden closed drawer scrollbar

## Rollback snapshot

Current pre-change snapshot:

`../gimpo-digitaltwin-main-ui-v21-rollback-before-hidden-closed-scrollbar`

## Rollback keyword

`rollback-before-hidden-closed-scrollbar`

## Manual rollback

If the hidden-scrollbar treatment for the closed drawer is not approved, replace the current `gimpo-digitaltwin-main-ui-v21` folder with the snapshot folder above.

The snapshot was created before hiding the visual scrollbar while preserving scroll behavior in the closed narrow drawer.
